За структурата Rectangle, декларирана по следния начин:
struct Rectangle {
  int length;        // дължина
  int width;        // широчина
};
да се дефинира:
а) функция void print_rectangle(Rectangle r), която извежда на екрана полетата на правоъгълника r;
б) функция void init_rectangle(Rectangle& r), която инициализира полетата на правоъгълника r чрез стойности, въведени от клавиатурата;
в) функция Rectangle init_rectangle(), която създава правоъгълник.
